extends ../layouts/default.pug

block title
  | النجم الثاقب

block content
  section.images
    for img in images
      figure
        img(src=`${imgroot}/ayah${img.src}.jpg`, alt=img.alt)
        figcaption= img.caption
  section.desc#Quran
    mixin text(className)
      .text(class=className)
        h1 Al Quran
        p
          | Al Quran is the noble and holy book sent by Allah
          br
          | (Subhanahu Wa Taa'la)
    +text('desktop')
    .photo-right
      figure
        img(src=`${imgroot}/Quran.jpg`, alt="Quran", width="320")
    +text('phone')
  section.desc#Hadith
    .photo-left
      figure
        img(src=`${imgroot}/Hadith.jpg`, alt="Hadith", width="320")
    .text
      h1 Al Hadith
      p
        | Al Hadith, the stories, are the noble words of the Prophet Muhammad (
        i peace be upon him
        | )
  section.img#img-1
    p
      | My Lord, build for me near You a house in Paradise
      br
      | (66:11)
  section.desc#prog-java
    mixin text(className)
      .text(class=className)
        h1 Java
        p Java, a widely popular programming language, programming is very interesting to learn with it.
    +text('desktop')
    .photo-right
      figure
        img(src=`${imgroot}/Java.jpg`, alt="java", width="320")
    +text('phone')
  section.desc#prog-py
    .photo-left
      figure
        img(src=`${imgroot}/Python.jpg`, alt="py", width="320")
    .text
      h1 Python
      p Python, the most popular programming language of the world, is also the easiest to learn.
  section.img#img-2
    p I never want the dependency graph to direct and point to me
