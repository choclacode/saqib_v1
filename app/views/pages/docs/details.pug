extends ../../layouts/default.pug

block title
  | #{doc.title} &laquo; #{doc.author[1]}

block desktop
  h2= doc.title

block content
  section.docs-content
    article
      for c in doc.content
        p !{c}
    a(href=`/docs/${category}`)
      .button Back
