extends ../../layouts/default.pug

block title
  | Docs

mixin main(className)
  .doc(class=className)
    .row
      for link in links
        a(href=`/docs/${link.href}`)
          span= link.name

block desktop
  +main('desktop')

block content
  section.phone
    +main('phone')
